name: shopping_list_env
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - requests # Added
  - certifi  # Added
  - pip:
    - streamlit
    - pandas
    - google-api-python-client
    - google-auth
    - google-auth-oauthlib
